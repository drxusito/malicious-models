apiVersion: v1
kind: Pod
metadata:
  name: infected-models-onnx
  namespace: malicious-models
spec:
  containers:
    - name: infected-models-onnx
      image: europe-west2-docker.pkg.dev/central-registry/conteval/raradwanski/infected-models:onnx
      command: ["sleep", "infinity"]
  restartPolicy: Never
